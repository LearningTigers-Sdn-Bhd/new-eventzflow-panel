/**
 * Shared character limit for the free-text profile fields used by both
 * hosts and bookers — Description/Bio, Sourcing Intent, and Capabilities.
 * Long enough for a real paragraph, short enough to stay readable in the
 * cards/tooltips these show up in and in the generated report.
 */
export const PROFILE_TEXT_FIELD_MAX_LENGTH = 500;
